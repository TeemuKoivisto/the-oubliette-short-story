\usepackage[activate={true,nocompatibility},final,tracking=true,kerning=true,spacing=true,factor=1100,stretch=10,shrink=10]{microtype}   % Better typography

\usepackage{lettrine}   % Add drop caps to chapter openings
\usepackage{graphicx}
\usepackage{ebgaramond}

\newcommand{\hsp}{\kern 1pt}    % for nested quotation marks

% Paragraph formatting
\renewcommand{\baselinestretch}{1.125}      % Allow paragraphs to breathe by spreading the lines further
\setlength{\parskip}{6pt}                   % Fixed space between paragraphs (i.e. disable variable parskip)
\setlength{\parindent}{1em}

%\renewcommand{\l}{mg}{
%  \lettrine[nindent=2.4pt, findent=-1.5pt, loversize=0.06]{\textbf{#1}}
%}
%\RenewDocumentCommand{\l}{mg}{
%    \lettrine[nindent=2.4pt, findent=-1.5pt, loversize=0.06]{\textbf{#1}}{\IfValueTF{#2}{#2}{}}
%}

% Don’t add extra space after sentences
\frenchspacing

% Reduce widows/orphans
\widowpenalty=10000
\clubpenalty=10000

% Page size
\usepackage[
    paperheight=9in,
    paperwidth=6in,
    top=0.75in,
    bottom=0.75in,
    outer=1in,
    inner=1in
]{geometry}

% Reduce overfull \hbox{} warnings
\sloppy

% Contents page
\usepackage[titles]{tocloft}
\renewcommand{\cftchapfont}{\large\itshape}
\renewcommand{\cftchappagefont}{\normalfont}
\renewcommand{\numberline}[1]{}

% chapter headings
\renewcommand\thechapter{\Roman{chapter}}
\usepackage[center,sc]{titlesec}
% Hack to remove chapter headers with an eye-balled negative vertical margin
\titleformat{\chapter}[display]
  {}{\vspace{-1cm}}{0pt}{\filcenter\Huge\scshape}

% headers/footers
\usepackage{fancyhdr} % Permits header customization. See header section below.
%\setlength{\headheight}{15pt}
%\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt} % No underline in the page header
\fancyhf[CFE,CFO]{\footnotesize\thepage} % Set page numbers in the footer
\fancyhead[CE]{\scriptsize\scshape\thechapter} % Chapter number in middle of left-side page header
\fancyhead[CO]{\scriptsize\scshape\booktitle} % Book title in middle of right-side page header
